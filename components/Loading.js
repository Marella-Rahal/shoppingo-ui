import React from "react";

const Loading = () => {
  return (
    <h2 className="w-full h-full flex justify-center items-center dark:text-darkTextColor">
      Loading ...
    </h2>
  );
};

export default Loading;
